# Vite dev server proxies API/auth to this backend (default: http://127.0.0.1:4002)
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:4002

# Leave empty for same-origin via proxy. Set only if the browser should call a remote host directly.
# VITE_API_ORIGIN=
